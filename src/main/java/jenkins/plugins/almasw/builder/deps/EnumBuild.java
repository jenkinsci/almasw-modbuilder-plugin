package jenkins.plugins.almasw.builder.deps;

public enum EnumBuild {	
	lastBuild,
	lastSuccessfulBuild,
	lastCompletedBuild,
	lastStableBuild,
	lastFailedBuild,
	lastUnstableBuild,
	lastUnsuccessfulBuild,
	jobId;
}
